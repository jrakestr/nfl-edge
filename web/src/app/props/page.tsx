import { EmptyState } from "@/components/EmptyState";

export const metadata = { title: "Props" };

export default function Page() {
  return <EmptyState title="Props">Prop detail lives at /props/[game]/[player]; the list arrives with step 6 (props and showdown).</EmptyState>;
}
