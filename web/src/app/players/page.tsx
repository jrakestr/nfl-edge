import { EmptyState } from "@/components/EmptyState";

export const metadata = { title: "Players" };

export default function Page() {
  return <EmptyState title="Players">Player projections and distribution sparks from model.proj_players. Fills in with step 6 (props view).</EmptyState>;
}
