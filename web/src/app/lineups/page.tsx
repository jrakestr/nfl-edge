import { EmptyState } from "@/components/EmptyState";

export const metadata = { title: "Lineups" };

export default function Page() {
  return <EmptyState title="Lineups">Lineup review per site and slate. Fills in with step 5 (DFS export and optimizer).</EmptyState>;
}
